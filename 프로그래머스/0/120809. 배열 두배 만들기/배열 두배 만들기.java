class Solution {
    public int[] solution(int[] numbers) {
        int solution[] = new int[numbers.length];
        
        
        for(int i = 0; i<numbers.length; i++)
        solution[i]=(numbers[i]*2);
        
        return solution;
        
    }
}